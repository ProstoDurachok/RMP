<!-- rounded_image.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="1000dp" /> <!-- Радиус закругления в dp -->
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Цвет и толщина границы -->
</shape>
